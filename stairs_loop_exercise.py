import random

n = random.randint(0, 100) 
stairs = list(range(0, n))
print(stairs)
steps = list()
for stairs in range(n):
    for each_element in stairs:
        each_element = str(stairs)
        each_element = stairs.append('I')
print(each_element, end="/t")

    
    
  
  
