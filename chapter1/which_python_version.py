import sys
"""import sys module from the Stabdard Library. For the running python interpreter as a program, from the sys module invoke 'sys.version' function and print the outcome to the user""" 
print(sys.version)
